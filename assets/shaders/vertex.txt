#version 450 core

layout(location = 0) in vec3 aPosition;
layout(location = 1) in vec2 aTexCoord;

out vec2 vTexCoord;

uniform mat4 uMVP;

void main()
{
    vec4 worldPosition = uMVP * vec4(aPosition, 1.0);
    vTexCoord = aTexCoord;
    gl_Position = worldPosition;
}